var React = require('react'),
    Router = require('react-router');

var Answer = React.createClass({
  render() {
    return (
      <div>
        Answer
      </div>
    );
  }
});

module.exports = Answer;
